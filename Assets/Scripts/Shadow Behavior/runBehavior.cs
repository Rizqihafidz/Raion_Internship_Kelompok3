using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class runBehavior : StateMachineBehaviour
{

    //public int speed;
    //private Transform playerPos;

    //override public void OnStateEnter(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
    //{
    //    playerPos = GameObject.FindGameObjectWithTag("Player").GetComponent<Transform>(); ;
    //}

    //override public void OnStateUpdate(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
    //{
    //    Vector2 target = new Vector2(playerPos.position.x, animator.bodyPosition.y);
    //    animator.transform.position = Vector2.MoveTowards(animator.bodyPosition, playerPos.position, speed * Time.deltaTime);
    //}

    //override public void OnStateExit(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
    //{

    //}


}
